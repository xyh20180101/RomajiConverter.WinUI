<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RomajiConverter.WinUI.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RomajiConverter.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <CommandBar Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Margin="8,8,8,4"
                    Padding="0" DefaultLabelPosition="Right"
                    HorizontalAlignment="Left" Height="44">
            <AppBarButton x:Name="ImportCloudMusicButton" Icon="Document" Label="获取网易云歌词" Click="ImportCloudMusicButton_OnClick"/>
            <AppBarSeparator/>
            <AppBarButton x:Name="ReadButton" Icon="OpenFile" Label="打开"/>
            <AppBarButton x:Name="SaveButton" Icon="Save" Label="保存"/>
            <AppBarButton x:Name="ConvertPictureButton" Icon="Pictures" Label="导出图片"/>
            <AppBarSeparator/>
            <AppBarToggleButton x:Name="DetailModeButton" Icon="List" Label="详细模式"/>
            <AppBarButton x:Name="SettingButton" Icon="Setting" Label="设置"/>
        </CommandBar>
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="InputTextBox" Grid.Row="0" Margin="8,4,4,4"
                 AcceptsReturn="True" TextWrapping="Wrap" 
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 IsSpellCheckEnabled="False"></TextBox>
            <CheckBox x:Name="AutoVariantCheckBox" Grid.Row="1" Margin="8,4,4,8" Content="识别变体"/>
            <Button x:Name="ConvertButton" Grid.Row="1" Content="转换" HorizontalAlignment="Right" Margin="4,4,4,8" Click="ConvertButton_OnClick"></Button>
        </Grid>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Expander Margin="4,4,4,4" Grid.Row="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,16,16,8" Header="显示选项">
                <StackPanel>
                    <VariableSizedWrapGrid Orientation="Horizontal">
                        <ToggleSwitch x:Name="EditRomajiCheckBox" MinWidth="100" Header="罗马音" IsOn="True"></ToggleSwitch>
                        <ToggleSwitch x:Name="EditHiraganaCheckBox" MinWidth="100" Header="平假名" IsOn="True"></ToggleSwitch>
                        <ToggleSwitch x:Name="IsOnlyShowKanjiCheckBox" MinWidth="100" Header="仅汉字假名" IsOn="True"></ToggleSwitch>
                    </VariableSizedWrapGrid>
                </StackPanel>
            </Expander>
            <ScrollViewer Grid.Row="1">
                <StackPanel x:Name="EditPanel" Margin="4,4,4,4">
                </StackPanel>
            </ScrollViewer>
            <Grid Grid.Row="2" Margin="4,4,4,8">
                <Button x:Name="ConvertTextButton" HorizontalAlignment="Right">生成文本</Button>
            </Grid>
        </Grid>
        <Grid Grid.Column="2" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Expander Margin="4,4,8,4" Grid.Row="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="16,16,16,8" Header="文本显示选项">
                <StackPanel>
                    <VariableSizedWrapGrid Orientation="Horizontal">
                        <ToggleSwitch x:Name="SpaceCheckBox" MinWidth="100" Header="空格分隔" IsOn="True"></ToggleSwitch>
                        <ToggleSwitch x:Name="NewLineCheckBox" MinWidth="100" Header="每句换行" IsOn="True"></ToggleSwitch>
                        <ToggleSwitch x:Name="RomajiCheckBox" MinWidth="100" Header="罗马音" IsOn="True"></ToggleSwitch>
                        <ToggleSwitch x:Name="HiraganaCheckBox" MinWidth="100" Header="平假名"></ToggleSwitch>
                        <ToggleSwitch x:Name="JPCheckBox" MinWidth="100" Header="日文" IsOn="True"></ToggleSwitch>
                        <ToggleSwitch x:Name="KanjiHiraganaCheckBox" MinWidth="100" Header="汉字假名标注"></ToggleSwitch>
                        <ToggleSwitch x:Name="CHCheckBox" MinWidth="100" Header="中文" IsOn="True"></ToggleSwitch>
                    </VariableSizedWrapGrid>
                </StackPanel>
            </Expander>
            <TextBox x:Name="OutputTextBox" Grid.Row="1" Margin="4,4,8,4" AcceptsReturn="True" TextWrapping="Wrap"
                         ScrollViewer.VerticalScrollMode="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         IsSpellCheckEnabled="False"></TextBox>
            <Grid Grid.Row="2" Margin="4,4,8,8">
                <Button HorizontalAlignment="Right">复制</Button>
            </Grid>
        </Grid>
    </Grid>
</Page>
